import { FilterByPlanetPipe } from './filter-by-planet.pipe';

describe('FilterByPlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
